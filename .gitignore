# Secrets / local config
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Node / Vite (InvenTree panel frontend)
node_modules/
frontend/dist/
# NB: inventree_kicad_assembly_panel/static/ is deliberately NOT ignored --
# it holds handwritten panel source today. Revisit in P2, when Vite lands and
# that directory becomes build output instead.

# Generated iBOM artifacts (regenerable per build order)
*.ibom.html
ibom_fields_*.xml

# OS
.DS_Store
**/.DS_Store
